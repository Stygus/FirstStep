server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # fallback na index.html dla SPA
    location / {
        try_files $uri $uri/ /index.html;
    }
}
